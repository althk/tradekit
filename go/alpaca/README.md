# alpaca

Planned module — not yet implemented.

See `../../tradekit-design.md` for what belongs here and which existing
project code is its donor. Nothing depends on this directory yet; it exists so
the module boundary is visible while `go/core` is being built out.
